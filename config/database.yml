default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

development:
  <<: *default
  database: project_auria_development

test:
  <<: *default
  database: project_auria_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>